# Wealth Horizon

> קובץ הנחיות לקלוד קוד. נוצר בשלב 0 של תוכנית המעבר לאנדרואיד.
> מקור האמת לתוכנית עצמה הוא `WEALTH_HORIZON_BRIEF.md` של אופק.

---

## 1. מטרת הפרויקט

אפליקציית **ניהול פיננסי אישי** לשימוש עצמי: תנועות, חשבונות, תקציבים, יעדים והשקעות.

**היעד שאליו אנחנו הולכים:**
- אפליקציית אנדרואיד אמיתית (APK), אייקון על מסך הבית — לא PWA ולא טאב בדפדפן.
- **ווידג'ט על מסך הבית** שמציג מבט חודשי: יתרה, הוצאה מול תקציב, 3 תנועות אחרונות.
- **100% אופליין.** כל הנתונים על המכשיר. חייב לעבוד במצב טיסה.
- אפס תלות בענן, אפס חשבון, אפס תשלום. גיבוי = ייצוא JSON ידני.

---

## 2. הסטאק — מה יש ולאן זה הולך

| רכיב | סטטוס |
|---|---|
| React 19 + Vite 6 + TypeScript + Tailwind 4 | ✅ נשאר |
| Recharts · lucide-react · motion | ✅ נשאר |
| `xlsx` | ✅ נשאר — **כבר בשימוש** ב-`Spreadsheet.tsx:3` לייבוא תנועות |
| `@capacitor-community/sqlite` | ✅ נכנס בשלב 2 — המסד המקומי |
| `jeep-sqlite` + `sql.js` | ✅ נכנס בשלב 2 — SQLite בדפדפן (פיתוח בלבד) |
| Firebase / Firestore | ✅ **הוסר בשלב 2** |
| `react-firebase-hooks` · `@google/genai` | ✅ **הוסרו בשלב 2** (היו תלויות מתות) |
| `server.ts` (Express) + `yahoo-finance2` | ✅ **הוסרו בשלב 2** — הכל עובר דרך `services/marketData.ts` |
| `vite-plugin-pwa` | ✅ **הוסר בשלב 3** |
| Capacitor + פלטפורמת android | ✅ נכנס בשלב 3 — `com.ofekb.wealthhorizon` |
| `@fontsource/*` | ✅ נכנס בשלב 3 — פונטים ארוזים, לא מ-CDN |
| ווידג'ט אנדרואיד | ✅ נכנס בשלב 4 — RemoteViews ב-Java, **לא** Glance (ראה §9) |

---

## 3. מבנה התיקיות

**נקודת כניסה:** `index.html` → `src/main.tsx` → `AuthProvider` → `ThemeProvider` → `VersionProvider` → `App`

`src/App.tsx` (1190 שורות) הוא הליבה: מחזיק את **כל** ה-state באובייקט `SpreadsheetState` יחיד,
מחזיק את כל ה-handlers של CRUD, ומנתב בין המסכים לפי `state.activeSheetId`
(רינדור מותנה — **אין react-router**).

```
src/
├── App.tsx                  ליבה: state גלובלי + ניתוב + כל ה-CRUD handlers
├── main.tsx                 נקודת כניסה, שרשור ה-Providers
├── types.ts                 מודל הנתונים + קטגוריות + מטבעות
├── index.css                Tailwind + 8 ערכות נושא
│                            (default/mono/forest/sunset/lavender/crimson/gold/royal)
├── components/
│   ├── Home.tsx             מסך בית — סיכום, patch notes, תזכורות, ייבוא state
│   ├── Dashboard.tsx        אנליטיקה + יעדים + תובנות AI
│   ├── Spreadsheet.tsx      טבלת תנועות (income/expenses)
│   ├── ImportDialog.tsx     ייבוא מהבנק — מיפוי עמודות, תצוגה מקדימה, כפילויות
│   ├── Accounts.tsx         CRUD חשבונות
│   ├── Investments.tsx      תיק השקעות, חיפוש טיקרים, גרפים
│   ├── Budgets.tsx          תקציבים לפי קטגוריה
│   ├── Settings.tsx         הגדרות + שפה + מפתח AI + ייצוא state ל-JSON
│   ├── LanguagePicker.tsx   בחירת שפה, בהרצה הראשונה בלבד
│   ├── AdminConsole.tsx     פאנל אדמין — רואה נתוני כל המשתמשים
│   ├── Login.tsx            Google Sign-In
│   ├── Sidebar.tsx / BottomNav.tsx / MobileTopBar.tsx    ניווט
│   ├── RemindersWidget.tsx  תזכורות שנשלחות במייל
│   ├── SpecialBackgroundEffect.tsx   אפקטי רקע
│   └── InstallModal.tsx     הנחיות התקנת PWA
├── data/                    ⭐ שכבת הנתונים
│   ├── types.ts             טיפוסי הישויות — מקור האמת
│   ├── Repository.ts        ה-interface: החוזה שכל מימוש חייב לספק
│   ├── SqliteRepository.ts  המימוש היחיד — SQLite מקומי
│   ├── schema.sql           סכימת גרסה 1
│   ├── tickers.ts           ⚠️ נוצר אוטומטית — 1612 סימולים לחיפוש מקומי
│   ├── migrations.ts        גרסאות סכימה לפי PRAGMA user_version
│   └── index.ts             נקודת החיבור + initDatabase()
├── context/
│   ├── ThemeContext.tsx     ערכת נושא — localStorage (נטען סינכרונית)
│   ├── LanguageContext.tsx  שפה + `useI18n()` — localStorage, סינכרוני
│   └── VersionContext.tsx   גרסת אפליקציה
├── i18n/                    ⭐ כל הטקסט שמוצג למשתמש
│   ├── en.ts                מקור האמת. כל מפתח כאן חייב להתקיים בשאר
│   ├── he.ts                עברית
│   ├── types.ts             `Dictionary = typeof en` — החוזה שכולן מקיימות
│   └── index.ts             LOCALES, getDictionary, categoryLabel וכו'
├── widget/
│   ├── snapshot.ts          בונה את ה-snapshot לווידג'ט (פונקציה טהורה)
│   ├── index.ts             הגשר ל-Capacitor, עם השהיה של 1.5ש'
│   └── __tests__/           19 בדיקות — `npm run test:widget`
├── native/
│   └── index.ts             עטיפת Capacitor — סרגל סטטוס, splash, כפתור "חזור"
└── services/
    ├── bankImport.ts        פענוח דפי בנק — לוגיקה טהורה, 53 בדיקות
    ├── marketData.ts        מחירים/חיפוש/אנליטיקה — ישירות מ-Yahoo, עם מטמון
    └── geminiService.ts     תובנות AI עם מפתח אישי מההגדרות
```

**7 מסכים** (`INITIAL_SHEETS`, App.tsx:26-34): Home · Accounts · Analytics · Income · Expenses · Budgets · Investments

**זרימת נתונים (אחרי שלב 2):**
`App.tsx` → `data/index.ts` → `Repository` (interface) → `SqliteRepository` → SQLite על המכשיר.
בנוסף `App.tsx` שומר עותק גיבוי של כל ה-state ב-`localStorage`.

**שתי שפות: אנגלית (ברירת מחדל) ועברית.** הבחירה מוצגת פעם אחת בהרצה
הראשונה (`LanguagePicker`) וניתנת לשינוי בהגדרות. עברית מפעילה RTL.
**התרגום עובר דרך `useI18n()` ולא דרך ייבוא ישיר** — ייבוא ברמת המודול
נקבע פעם אחת בטעינה ולא היה מתעדכן בשינוי שפה.

**התשתית בנויה לריבוי שפות בכוונה**, גם אחרי הצמצום לשתיים. הוספת שפה
היא קובץ מילון אחד ושורה אחת ב-`LOCALES`; `Dictionary` יאכוף שהמילון
מלא. זה רלוונטי אם האפליקציה תפורסם לקהל רחב בעתיד.

**אין התחברות ואין משתמשים.** האפליקציה נפתחת ישר לנתונים.
במקום שער ההתחברות יש שער פתיחת מסד: מסך טעינה עד שהמסד נפתח, ומסך שגיאה אם נכשל.

**רשת:** רק למחירי מניות ולתובנות AI. שתיהן נכשלות בשקט ולא חוסמות כלום.

---

## 4. מודל הנתונים

6 הישויות מ-`firebase-blueprint.json` — **לשמר אותן**:

| ישות | שדות מפתח |
|---|---|
| `Transaction` | id, date, description, category, amount, type?, accountId? |
| `Account` | id, name, type (Bank/Investment/Pension/Cash), balance |
| `Investment` | id, ticker, name?, exchange?, shares, avgPrice, currentPrice |
| `Goal` | id, name, targetAmount, currentAmount, deadline?, category |
| `Budget` | id, category, limit |
| `UserProfile` | activeSheetId, notes, patchNotes, theme |

**שתי ישויות נוספות ב-`src/types.ts` שאינן ב-blueprint** — לא לשכוח אותן בסכימת SQLite:
- `Reminder` — id, subject, body, scheduledTime, sent, recurrence?, dayOfMonth?
- `Sheet` — הגדרת מסך (UI בלבד, לא צריך טבלה)

הגדרת המקור: `src/types.ts`.

---

## 5. כללי עבודה — מחייבים בכל שלב

1. **branch לכל שלב.** שם: `phase-1-repository`, `phase-2-sqlite` וכו'.
2. **קומיט קטן אחרי כל שינוי לוגי.** הודעות באנגלית, פורמט `type: description`
   (למשל `refactor: extract data access into Repository interface`).
3. **הבילד חייב לעבור.** בסוף כל שלב: `npm run lint` ו-`npm run build` נקיים.
   אם משהו נשבר — לתקן לפני שממשיכים.
4. **אל תדלג שלבים.** כל שלב מניח שהקודם הושלם.
5. **בסוף כל שלב** — סיכום של 3–5 שורות: מה השתנה, למה, ומה אמורים לראות עכשיו.
6. **אל תמחק קוד בלי לוודא שהוא באמת לא בשימוש.**

### תקשורת
- **עברית.** האפליקציה עצמה עוד לא — ראה מלכודת #3.
- **הסבר כל מונח טכני בשורה אחת** בפעם הראשונה שמשתמשים בו.
- **הסבר כל פקודת git בשורה** — מה היא עושה בפועל.
- **קבל החלטות סבירות בעצמך.** לא מטח שאלות הבהרה. לשאול רק כשההחלטה בלתי הפיכה
  או תלויה במידע שרק אופק יודע.
- **קצר ולעניין.** בלי הקדמות.

---

## 6. מלכודות ידועות — נמצאו בשלב 0

### ✅ 1. תזכורות ומשוב לא נשמרים — נפתר בשלב 2
`firestore.rules` **אין בו כלל** ל-`users/{uid}/reminders` ולא ל-`feedback`, ואילו
`firestoreService.ts:204-222, 370-393` כותב לשניהם. הכלל הראשון בקובץ
(`match /{document=**} { allow read, write: if false }`) חוסם אותם.
`handleFirestoreError` (`lib/firestore-utils.ts:48`) מדפיס ל-console **וזורק מחדש**.
מכיוון שהקריאות ב-`App.tsx` לא מחכות ב-`await` ואין להן `.catch`, התוצאה היא
unhandled promise rejection: המשתמש לא רואה כלום, התזכורת נראית שנשמרה, ונעלמת ברענון.
**נפתר:** אין יותר Firestore ואין כללי הרשאה. תזכורות נשמרות בטבלה מקומית.
בדרך התגלה באג שני: `Settings` רינדר את ווידג'ט התזכורות עם `onAddReminder!`
בעוד ש-`App.tsx` לא העביר את ההנדלר — כלומר הוספת תזכורת זרקה שגיאה. תוקן.

> תוקן ב-2026-08-23: בשלב 0 כתבתי כאן ש-`handleFirestoreError` "רק מדפיס ל-console".
> זה לא נכון — הוא זרק. האפקט שהמשתמש חווה היה זהה, המנגנון שונה.

### 🟡 2. כתיבות בתוך `setState` — טופל חלקית
**היה:** ~45 קריאות ל-Firestore מפוזרות ב-`App.tsx`, רובן בתוך פונקציות עדכון של
`setState` — פונקציות שחייבות להיות טהורות. ב-React 19 StrictMode הן רצות פעמיים → כתיבה כפולה.

**עכשיו:** כל הגישה עוברת דרך ה-Repository, וכל כתיבה היא **upsert** — ולכן קריאה
כפולה כבר לא מזיקה. 15 כתיבות עדיין יושבות בתוך updaters, כולן בהנדלרים שמחשבים
מצב נגזר מ-`prev` (יתרות חשבון, עריכת שדה). ההערה ב-`App.tsx` מסבירה למה.

**מה שנשאר:** כל עריכת תנועה עדיין כותבת מחדש את *כל* החשבונות
(`nextAccounts.forEach`). ב-SQLite זה upsert מקומי ולא קריאת רשת, ולכן זה
כבר לא כואב — אבל זה עדיין בזבזני. שווה ניקוי בשלב עתידי.

### ✅ 3. האפליקציה חד-לשונית — נפתר בשלב 3.5, הורחב בשלב 7
שלב 3.5 תרגם את כל שבעת המסכים לעברית והמיר מחלקות Tailwind כיווניות
ל-logical properties. שלב 7 בנה תשתית רב-לשונית עם שש שפות; אופק ביקש
לצמצם לשתיים שהוא באמת משתמש בהן — **אנגלית (ברירת מחדל) ועברית** —
והתשתית נשארה על מכונה.

**כל הטקסט יושב ב-`src/i18n/`** ומגיע דרך `useI18n()` כ-`txt` (ולא `t`,
שתפוס כשם משתנה בתשע קומפוננטות). שמות קטגוריות וסוגי חשבונות מתורגמים
**בתצוגה בלבד** — הערכים במסד נשארים באנגלית, אחרת נתונים קיימים וקבצי
גיבוי ישנים היו נשברים.

**מה שלמדנו בשלב 7:** grep לא מספיק כדי למצוא טקסט שלא תורגם. אחרי
שהמסכים "נראו מתורגמים" נשארו בהם מחרוזות שהתגלו רק כשרינדרנו את
האפליקציה בדפדפן אמיתי וסרקנו את ה-DOM אחרי טקסט לטיני שנשאר — ביניהן
**כל דיאלוג מחיקת הנתונים**, שמות ערכות הנושא, אפקטי הרקע, טופס
התזכורות, ושם חודש שהיה נעוץ ב-`'he-IL'` ונשאר עברי בכל שפה.
הסקריפט יושב ב-`scripts/sweep-i18n.mjs`. **להריץ אותו אחרי כל הוספה
של טקסט.**

**פונטים:** רק Heebo ארוז, והוא מכסה עברית ולטינית כאחד. אם תתווסף
שפה בכתב אחר — נסמוך על פונטי המערכת של אנדרואיד (Noto) ולא נארוז,
כי אריזת CJK לבדה מוסיפה מיליוני בתים ל-APK.

### ✅ 4. שלושה service workers מתנגשים — נפתר בשלב 3
כל ה-PWA ירד: `sw.js`, `manifest.json`, `vite-plugin-pwa` ומודל ההתקנה.
`main.tsx` גם מבטל רישום של service worker ישן ומנקה מטמון, עבור מכשירים
שכבר פתחו את הגרסה הקודמת.
בדרך התגלה שגם **הפונטים** נמשכו מ-CDN חיצוני — אותה בעיה בדיוק. הם נארזים
עכשיו דרך `@fontsource`. **הבילד לא מבצע אף בקשת רשת חיצונית.**

### 🟡 5. נתונים מזויפים — שניים מתוך שלושה נפתרו
- ✅ סימולציית המחירים האקראית הוסרה. מה שמוצג הוא מחיר אמיתי שנמשך,
  או האחרון שנשמר במטמון.
- ✅ `cpuUsagePercent` המזויף נמחק יחד עם `server.ts` ו-`AdminConsole`.
- ✅ דירוג "Strong Buy / Sell" נוסח מחדש בשלב 3.5. החישוב לא השתנה, אבל
  התווית בעברית מתארת **איפה המחיר ביחס לממוצעים הנעים** במקום להורות לקנות
  או למכור. הערכים ב-`marketData.ts` עדיין באנגלית — הם מזהים פנימיים,
  והתרגום קורה ב-`trendLabel`.

### ✅ 6. חיפוש המניות לא מצא כלום — נפתר בשלב 7
אופק דיווח: "מופיע שם SPY אבל כשאני מחפש בעצמי הוא לא מוצא".
אלה היו **שני מנגנונים שונים**. ה-SPY שהוא ראה הוא רשת קיצורי דרך
קשיחה בקוד (`Investments.tsx`), שמוצגת רק כשתיבת החיפוש **ריקה** ולא
נוגעת ברשת. הקלדה החליפה אותה בחיפוש חי מ-Yahoo — ש-CORS חוסם בדפדפן,
ולכן החזיר רשימה ריקה **תמיד**. §7 תיעד את הנפילה למטמון עבור *מחירים*,
אבל לחיפוש לא הייתה שום נפילה אחורה.

בדרך נמצאו עוד ארבעה: כל חיפוש ירה **11 בקשות רשת** (גרף שנתי לכל אחת
מעשר התוצאות) **שלא הוצג בשום מקום** ורק הזמין חסימת קצב מ-Yahoo;
לא הייתה שמירה על סדר התשובות, כך שהקלדה עם עצירה באמצע יכלה להחזיר
תוצאה ישנה; כל תוצאה ריקה הוצגה כשגיאת רשת; ו-`selectTicker` היה קוד מת.

**נפתר:** `src/data/tickers.ts` — קטלוג ארוז של 1612 סימולים שעונה
מיידית בלי רשת. Yahoo נשאר כתוספת בלבד.

### מפתחות API — נסגר בשלב 2
- ✅ `firebase-applet-config.json` נמחק יחד עם Firebase.
- ✅ מפתח Gemini: נבחרה **אפשרות ב׳** מהבריף. מזינים מפתח אישי במסך ההגדרות,
  הוא נשמר ב-localStorage על המכשיר בלבד, ונשלח רק ל-Google. בלי מפתח הפיצ'ר
  כבוי ולא חוסם כלום. ה-`define` ב-`vite.config.ts` שהיה מטמיע מפתח בבאנדל הוסר.
- ✅ טוקן Gmail נמחק. שליחת תזכורות במייל ירדה יחד עם ההתחברות ל-Google —
  התזכורות נשמרות ומוצגות באפליקציה, והתראה אמיתית תיכנס בשלב 4.

---

## 7. רשת — מה עדיין יוצא החוצה

`server.ts` נמחק בשלב 2. שתי קריאות רשת נשארו, שתיהן אופציונליות:

| מה | לאן | כשאין רשת |
|---|---|---|
| מחירים · אנליטיקה | Yahoo, ישירות מהמכשיר (`CapacitorHttp`) | מחזיר את המחיר המוטמן האחרון |
| חיפוש סימולים | Yahoo — **תוספת בלבד** | הקטלוג המקומי עונה לבדו |
| תובנות AI | Gemini **או** Claude, לפי הספק שנבחר, עם מפתח אישי מההגדרות | הפיצ'ר כבוי בשקט |
| אחזקות מהברוקר | Interactive Brokers Flex, **קריאה בלבד**, לפי בקשה מפורשת | מוצגת הודעה, התיק לא משתנה |

**חוק:** אף אחת מהן לא זורקת, לא מחזירה 0, ולא חוסמת שום מסך.
בדפדפן Yahoo חוסם CORS ולכן המחירים לא יתעדכנו בפיתוח — באנדרואיד זה עובד.
**חיפוש הסימולים כן עובד בדפדפן**, כי הוא לא תלוי ב-Yahoo (ראה מלכודת #6).

מפתחות ה-API נשמרים ב-localStorage **על המכשיר בלבד**, כל אחד נשלח רק
לספק שלו, ואף אחד מהם לא נכנס לבאנדל ולא לגיבוי.

## 8. פקודות

```bash
npm install       # התקנת תלויות (postinstall מעתיק את sql-wasm.wasm)
npm run dev       # שרת פיתוח vite
npm run lint      # tsc --noEmit — בדיקת טיפוסים
npm run build     # vite build → dist/
npm run preview   # תצוגה מקדימה של הבילד
```

**אנדרואיד** — הוראות מלאות ב-`README-ANDROID.md`:

```bash
npm run build && npx cap sync   # ⚠️ שכחת sync = הטלפון מציג גרסה ישנה
npx cap open android            # פותח את Android Studio
```

```bash
npm test                  # כל הבדיקות (128)
npm run test:widget       # 19 בדיקות ל-buildSnapshot
npm run test:import       # 53 בדיקות לייבוא מהבנק
npm run test:search       # 22 בדיקות לחיפוש סימולים — כולן בלי רשת
npm run test:i18n         # 9 בדיקות לשלמות המילונים
npm run test:broker       # 25 בדיקות לפענוח דוח ה-Flex של IBKR
npm run sweep:i18n        # מרנדר בעברית בדפדפן ומחפש אנגלית שנשארה
npm run build:demo        # קובץ HTML יחיד שאפשר לפתוח מכל מכשיר
npm run build:artifact    # אותו קובץ בלי מעטפת מסמך, לפרסום ככתובת
npm run verify:android    # תקינות XML + הפניות למשאבים (לא בנייה)
npm run check:java        # מקמפל את קוד הווידג'ט מול פלטפורמת אנדרואיד
```

**חובה בסוף כל שלב:** `npm run lint && npm run build` — שניהם נקיים.

---

## 9. הווידג'ט — למה RemoteViews ולא Glance

הבריף ביקש Jetpack Glance ב-Kotlin. נבחר **RemoteViews ב-Java**.

**הסיבה:** Glance מחייב תוסף Kotlin + מהדר Compose + תלות Glance, ושלושתם
צריכים להיות מיושרים מול AGP. אי אפשר לקמפל בסביבת הפיתוח הזו (אין Android
SDK), ואי-התאמת גרסאות הייתה משאירה את אופק מול שגיאות Gradle. הווידג'ט הוא
טקסט ופס התקדמות — RemoteViews מספיק, בלי להוסיף כלום לשרשרת הבנייה.

**מתי כן לעבור ל-Glance:** אחרי שהווידג'ט מוכח כעובד על המכשיר, ואם רוצים
Material You דינמי או פריסות מורכבות יותר.

**ארכיטקטורה:** הווידג'ט לא ניגש ל-SQLite ולא לרשת. האפליקציה כותבת
`snapshot` ל-SharedPreferences אחרי כל שינוי, והווידג'ט קורא רק אותו.
`updatePeriodMillis=0` — הוא לא מתעורר לבד ולא מעיר את המכשיר.

**מה נבדק בלי SDK:** `npm run check:java` מקמפל את כל שש המחלקות לבייטקוד
מול `android.jar` אמיתי (API 23 מ-apt — הפלטפורמה הגבוהה ביותר שקיימת
כחבילת מערכת; הקוד לא משתמש בכלום מעליה, ו-minSdk הוא 24). ה-`R.java`
נגזר מהמשאבים שבפרויקט, ולכן הפניה למשאב חסר נופלת שם.
**אריזת APK עדיין דורשת את ה-SDK המלא מ-`dl.google.com`.**

**מציג שעה מוחלטת** ("עודכן 14:32") ולא "לפני X דקות": הווידג'ט מתרנדר רק
כשהנתונים משתנים, וזמן יחסי היה נתקע ונשאר שגוי.

---

## 10. ייבוא מהבנק — מלכודות שכבר נפלנו בהן

**`new Date('03/08/2026')` שקרי.** הוא מפרש חודש/יום לפי התקן האמריקאי,
בעוד שכל הבנקים בישראל מייצאים יום/חודש. 3 באוגוסט הפך ל-3 במרץ בשקט
מוחלט. `parseDate` ב-`bankImport.ts` מניחה **יום לפני חודש** תמיד.

**שורת הכותרות לא תמיד ראשונה.** דפי בנק פותחים בשם הבנק ומספר החשבון.
`findHeaderRow` סורקת 25 שורות ובוחרת את זו עם הכי הרבה מילות כותרת מוכרות.

**שתי צורות של סכום.** או עמודה אחת עם סימן, או `חובה`/`זכות` נפרדות.
חובה = יוצא = שלילי, זכות = נכנס = חיובי.

**`תאריך` לפני `תאריך ערך`.** דף בנק מכיל לעתים את שניהם. תאריך העסקה
הוא הרלוונטי; תאריך ערך הוא מתי הבנק זיכה בפועל. סדר הכינויים
ב-`ALIASES.date` מכוון.

**שורות ריקות נשמרות בקריאה.** בלעדיהן מספרי השורות בהודעות הדילוג
לא תואמים למה שהמשתמש רואה באקסל.

**זיהוי כפילויות:** תאריך + תיאור מנורמל + סכום. בודק גם מול הקיים וגם
בתוך הקובץ עצמו. **המשתמש מאשר, לא המערכת** — שתי עסקאות אמיתיות זהות
באותו יום ייראו כמו כפילות.

---

## 11. חיפוש סימולים — למה יש קטלוג ארוז

`src/data/tickers.ts` מחזיק 1612 סימולים ונוצר על ידי
`scripts/generate-tickers.py`. **הקובץ מקומט, והבנייה לא מושכת אותו מהרשת.**

**הסדר קובע.** `searchCatalog` עונה **סינכרונית מהקטלוג** בכל הקלדה,
ורק אחר כך `searchTickers` ממזג מעליו את מה ש-Yahoo החזיר. זה הפוך
מהמצב הקודם, שבו Yahoo היה המקור היחיד — ולכן החיפוש לא עבד בדפדפן
בכלל, ובאנדרואיד נשבר בכל נפילת רשת או חסימת קצב.

**לא למשוך גרפים בתוך החיפוש.** הגרסה הקודמת משכה גרף שנתי לכל אחת
מעשר התוצאות — 11 בקשות להקלדה אחת — ואף אחד מהם לא הוצג. הגרפים
נמשכים רק לניירות שכבר בתיק, דרך `fetchAnalytics`.

**כל תשובה אסינכרונית חייבת שומר סדר.** `latestQuery` ב-`Investments.tsx`
זורק תשובה שאינה של השאילתה הנוכחית. בלעדיו הקלדה עם עצירה באמצע
מחזירה תוצאה שונה תלוי בקצב ההקלדה.

**לרענון הקטלוג** (פעם בשנה, כשמדד S&P משתנה):
`python3 scripts/generate-tickers.py`

---

## 12. מה מתוכנן — ביקשת לזכור

בקשות שאופק העלה ושטרם נבנו. **לא לבנות בלי לדבר איתו קודם.**

### בנקאות פתוחה ומשיכת נתונים מברוקר
> *"אני רוצה להוסיף אופציה לבנקאות פתוחה וכדומה, אולי גם הבאת נתונים
> מהברוקר או אפילו חיבור של read only אליו."*

שלוש בקשות נפרדות בהיקף עולה:
1. ✅ **חיבור read-only לברוקר** — **נבנה.** ראה §13.
2. **משיכת נתונים מברוקר** — גם היסטוריית עסקאות. עוד לא.
3. ❌ **בנקאות פתוחה** — חסומה. ראה למטה.

**בנקאות פתוחה חסומה לאדם פרטי, וזו לא שאלה של מאמץ.** בישראל היא
מוסדרת ב**חוק שירות מידע פיננסי, תשפ"ב-2021**, וגישה ל-API של הבנקים
ניתנת **רק לחברות בעלות רישיון** מרשות ניירות ערך — נכון לבדיקה, ארבע
חברות בסך הכל. אין מודל של "מפתח API אישי לחשבון שלי", בניגוד ל-Gemini
או ל-IBKR. המסלול היחיד הוא אגרגטור מורשה בתשלום חודשי, מול "אפס תשלום"
ו"אפס ענן" בבריף. הייבוא מאקסל (`bankImport.ts`) קיים ועובד בדיוק בגלל זה.

**ברוקר, לעומת זאת, כן אפשרי** — כי IBKR נותנים טוקן אישי לחשבון שלך.
זה ההבדל, וזו הסיבה ש-§13 קיים ובנקאות פתוחה לא.

### היסטוריית עסקאות מהברוקר
היום נמשכות **אחזקות פתוחות בלבד**. אותה שאילתת Flex יכולה להחזיר גם
`Trades`, ומשם אפשר לייצר תנועות. זה שינוי גדול יותר כי צריך זיהוי
כפילויות מול תנועות קיימות — בדיוק הבעיה ש-`bankImport.ts` כבר פתר,
ושווה לעבור על הפתרון שם לפני שכותבים חדש.

### פורמט מספרים ותאריכים לפי שפה
היום סכומים מוצגים בפורמט `en-US` בכל השפות (ספרות מערביות בכל מקום),
ורק **שמות חודשים ותאריכים** הולכים לפי השפה הפעילה. מעבר מלא ל-`Intl`
יגרור ספרות הודיות-ערביות בערבית ופורמט אחר בכל שפה — שינוי שדורש
מעבר על כל מסך, ולכן לא נכנס יחד עם התרגום.

### שלב 5 — עיצוב
לא התחיל. לפי הבריף, אופק בוחר כיוון עיצובי לפני שנוגעים.

---

## 13. חיבור לברוקר — IBKR Flex, קריאה בלבד

`src/services/broker.ts`. **קריאה בלבד: האפליקציה לא יכולה לבצע פעולות
ולא להזיז כסף.** זו לא רק הצהרה — Flex Web Service הוא ממשק דוחות,
ואין בו נקודת קצה למסחר.

**איך זה מסתדר עם "אפס ענן":** אין שרת ואין צד שלישי. מייצרים בחשבון
ה-IBKR טוקן אישי (תקף עד שנה) ומזהה שאילתה, מדביקים בהגדרות, והם
נשמרים ב-localStorage על המכשיר — בדיוק כמו מפתח ה-AI.

**הפרוטוקול שני שלבים:** `SendRequest` מחזיר `ReferenceCode`, ואז
`GetStatement` מחזיר את הדוח. בין השניים השרת מייצר אותו, ולכן שלב 2
עשוי להחזיר קוד 1019 ("בהכנה") — מנסים עד חמש פעמים בהפרש 2 שניות.

**שגיאות מוצגות עם הקוד וההודעה המקורית של IBKR באנגלית.** כותרת
מתורגמת, פירוט כלשונו. `1015` = טוקן שגוי, `1012` = טוקן פג, `1013` =
הגבלת IP, `1014` = שאילתה לא תקינה.

**מיזוג לתיק: לפי סימול, ואף פעם לא מחיקה.** אחזקה קיימת מתעדכנת
ושומרת את ה-id; חדשה נוספת; מה שלא בדוח נשאר. **המשתמש רואה מה עומד
להשתנות ומאשר** — אותו עיקרון של דיאלוג הייבוא מהבנק, כי גם כאן
מדובר בדריסת נתונים פיננסיים.

**`parseSendRequest` ו-`parsePositions` טהורות ונבדקות** (25 בדיקות,
`npm run test:broker`) — בלי רשת. הפענוח דרך `fast-xml-parser` ולא
דרך `DOMParser` בדיוק בשביל זה: אותו קוד רץ בדפדפן וב-node.

⚠️ **בדפדפן IBKR חוסמים CORS**, כמו Yahoo. המשיכה עובדת באנדרואיד.
